/**
 * @base-ts/core — A TypeScript library starter template.
 *
 * Export your public API from this file.
 * Keep internal implementation details in `src/internal/`.
 */

export { greet, range, sum } from "./internal/utils.ts"
